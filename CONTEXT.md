# pitr-fs 领域语言

pitr-fs 为普通 POSIX 文件操作提供自动历史、恢复和面向多 Agent 的隔离协作能力。
本文统一描述版本与访问边界，避免将部署入口误认为数据或版本的所有者。

## 工作空间与版本

**工作空间（Workspace）**：
拥有独立文件命名空间、版本时间线和策略的逻辑资源边界；同一工作空间可以通过多个挂载点访问。
_避免使用_：挂载点、目录、卷

**工作空间修订（Workspace Revision）**：
工作空间时间线中不可变的状态标识；同一工作空间的所有挂载点共享对其修订及当前版本的认识。
_避免使用_：挂载点版本、节点版本

## 访问入口

**挂载点（Mount）**：
工作空间或其特定视图的 POSIX 访问入口；它不独立拥有文件数据、策略或版本时间线。
_避免使用_：工作空间、版本域
